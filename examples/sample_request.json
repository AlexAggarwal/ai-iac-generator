
# ===================== examples/sample_request.json =====================
{
  "natural_language": "Deploy a 3-tier web app in Azure with Application Gateway, two VMs in an availability set, and a backend Azure SQL database."
}

# ===================== output/main.tf (example output) =====================
# This would be generated by the API dynamically
provider "azurerm" {
  features {}
}

resource "azurerm_resource_group" "rg" {
  name     = "webapp-rg"
  location = "East US"
}

# ... VNet, Subnets, App Gateway, VM Scale Set, SQL Server resources etc.
